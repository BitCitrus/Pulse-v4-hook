# 审查记录

本文记录合约审查发现的问题、验证方式与处置结果。当前行为以 [SPEC](SPEC.md) 为准。

## 结论摘要

审查从一个"动态费率 + 共享窄区间流动性金库"的实现开始,**最终结果是移除整个金库**。
决定依据是可复现的测量,不是风格判断。

## 严重:再平衡可在操纵价格上被强制换币

**位置**:已移除的 `RebalanceEngine._balanceInventory` 与 `InventoryPlanner`。

金库在**池内**做市价换币来调整库存比例,而唯一的滑点保护是新区间自己的边界——
这允许该笔换币把价格推过整个新区间。同时 `rebalance()` 完全无许可,没有任何价格合理性校验。

攻击者可以自造"需要再平衡"的状态,强迫金库在自己操纵出的价格上买入,再把价格推回。

**实测**(1e21 存款,背景流动性 1e21,最终价格回到 1:1):

| 场景 | 攻击者盈亏 | 金库价值变化 |
|---|---:|---:|
| 无金库,纯往返交易 | −0.021e18 | — |
| 有金库,不调用 `rebalance()` | −1.42e18 | **+1.00e18** |
| 往返中间调用 `rebalance()` | **+41.13e18** | **−41.65e18** |

对照组证明因果明确:插入一次 `rebalance()` 调用,攻击者从亏 1.42e18 变成赚 41.13e18。
**单次往返 = 金库 TVL 的 2.8%,十次往返损失 44%**,且可在**单笔交易内原子完成**。

同一条换币路径也被 `deposit()` 触发,用诚实用户的存款当触发器攻击者盈利 **+51.2e18**,
因此只限制 `rebalance()` 入口无效。

## 中危:暂停开关瘫痪整个池子

`afterSwap` 带 `whenNotPaused`,owner 暂停后该池**所有交易全部 revert**,
把与金库无关的普通交易者和普通 LP 一并锁死。

**已修复**:暂停改为只让协议费归零,绝不阻断交易。

## 中危:费率缓存窗口可被单方固定

旧设计中 `beforeSwap` 使用缓存费率,超过 120 秒才刷新,而 `pokeFee()` 无许可、冷却仅 60 秒。
这意味着**任何人都能决定随后 120 秒内所有人支付的费率**。

**已修复**:改为每区块首笔交易自动重算,窗口归零;`pokeFee` 随之失去意义并被删除。

## 尝试过的修复,以及为什么不够

**非原子化门槛**(要求越界状态持续 N 个区块才能维护):挡住了原子攻击(攻击者从 +41.13e18
变成 −1.42e18),但攻击者只要熬过延迟窗口仍能抽走 2.5%。防御依赖"套利者会在窗口内纠正价格"
这个经济假设,不是代码保证。

**取消强制换币,改为单边建仓**:把可提取量砍到 1.5%,消除了换币机制本身,
但没有归零——金库仍然把流动性重新放在被操纵的价格附近,价格回弹时被碾。

## 决定性证据:维护本身就是亏钱的

用多角色市场模拟(226 笔随机散户交易 / 120 笔协同拉盘),按结束价格计价并与 HODL 对比:

| 场景 | keeper | LP 价值 | HODL 价值 | 保住 |
|---|---|---:|---:|---:|
| 随机散户流 | **开** | 426.8e18 | 1012.1e18 | **42%** |
| 随机散户流 | 关 | 989.7e18 | 991.6e18 | **99.8%** |
| 协同拉盘 | **开** | 53.7e18 | 522.3e18 | **10%** |
| 协同拉盘 | 关 | 398.5e18 | 599.1e18 | 67% |

**关掉维护,LP 几乎完全跟住 HODL;开着维护,亏掉 58%。**

机制:单边挂单加"被甩开就追"的组合,在上涨趋势里会一路把买单抬高,保证买在最高点。
排除了会计 bug——关闭 keeper 时资金无泄漏,全量退出后账目精确归零,数百笔交易零 revert。

宽度扫描进一步显示,维护从负价值转正的拐点在 7 个 tick-spacing 附近,
而在均值回归路径上小赚、在趋势路径上巨亏——**用单一路径调参本身就不可靠**。

## 处置

金库、份额凭证、再平衡引擎与 keeper 奖励池**全部移除**。随之消失的不只是这个漏洞,
还有整类失败模式:没有金库可被强制换币,没有份额可被稀释,没有跨池预算可被串用。

## 归档

- [早期审查全文](docs/history/review20260913.md)
- [金库历史问题回归记录](docs/history/recheck20260913.md)
- [奖励池设计草案](docs/history/rewards20260913.md)

归档内容对应已移除的实现,**不能作为当前代码的结论**。
